# variable name should be meaningful
# variable name can have only alpha numeric characters
# space, # , ? (special symbol rather than underscore) are not allowed
#variable can not start with number(digit)
#it can start with letter, underscore
